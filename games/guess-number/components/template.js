import React from 'react';
import { View, Text, StyleSheet, Button } from 'react-native';

const template = props => { return (<View></View>)};

const styles = StyleSheet.create({});

export default template;